<Page x:Class="FluentFlyoutWPF.Pages.TaskbarVisualizerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:MicaWPF.Controls;assembly=MicaWPF"
      xmlns:viewModels="clr-namespace:FluentFlyoutWPF.ViewModels"
      xmlns:fluentflyoutcontrols="clr-namespace:FluentFlyout.Controls"
      mc:Ignorable="d"
      d:DesignHeight="1000" d:DesignWidth="800"
      Title="TaskbarWidgetPage"
      d:DataContext="{d:DesignInstance viewModels:UserSettings}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      d:Background="White">

    <StackPanel MaxWidth="1000" Margin="16">
        <TextBlock Text="{DynamicResource TaskbarVisualizerTitle}" FontSize="24" FontWeight="SemiBold" FontFamily="Segoe UI Variable" Margin="0,0,0,20" />
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,24">
                <Border CornerRadius="10" ClipToBounds="True" Width="164" Height="98">
                    <Border.Background>
                        <ImageBrush ImageSource="/Resources/FluentFlyoutTaskbar-NoTitle-Zoomed.png" Stretch="UniformToFill" />
                    </Border.Background>
                </Border>
                <TextBlock FontSize="14" FontWeight="Regular" HorizontalAlignment="Stretch" Opacity="0.75" Margin="12,0,0,0" MaxWidth="450" TextWrapping="WrapWithOverflow" Text="{DynamicResource TaskbarVisualizerDescription}" />
            </StackPanel>

            <!-- Premium Status InfoBar (only shown for Store versions) -->
            <ui:InfoBar Grid.Row="1" Title="{DynamicResource PremiumUnlockedMessage}" Severity="Success" IsClosable="True" Margin="0,0,0,3">
                <ui:InfoBar.Style>
                    <Style TargetType="ui:InfoBar" BasedOn="{StaticResource {x:Type ui:InfoBar}}">
                        <Setter Property="IsOpen" Value="False" />
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsStoreVersion}" Value="True" />
                                    <Condition Binding="{Binding IsPremiumUnlocked}" Value="True" />
                                </MultiDataTrigger.Conditions>
                                <Setter Property="IsOpen" Value="True" />
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </ui:InfoBar.Style>
            </ui:InfoBar>

            <!-- Premium Locked InfoBar (only shown for Store versions without premium) -->
            <ui:InfoBar Grid.Row="1" IsOpen="True" Severity="Warning" IsClosable="False" Margin="0,0,0,3">
                <ui:InfoBar.Style>
                    <Style TargetType="ui:InfoBar">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsStoreVersion}" Value="True" />
                                    <Condition Binding="{Binding IsPremiumUnlocked}" Value="False" />
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible" />
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </ui:InfoBar.Style>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{DynamicResource PremiumLockedMessage}" VerticalAlignment="Center" />
                    <fluentflyoutcontrols:PremiumPurchaseButton Margin="12,2,0,2" />
                </StackPanel>
            </ui:InfoBar>

            <!-- GitHub Version InfoBar -->
            <ui:InfoBar Grid.Row="1" Title="{DynamicResource PremiumGitHubMessage}" Severity="Informational" IsOpen="True" IsClosable="False" Margin="0,0,0,3" Visibility="{Binding IsStoreVersion, Converter={StaticResource BoolToCollapsedVisibleConverter}}" />
        </Grid>

        <StackPanel Opacity="{Binding IsPremiumUnlocked, Converter={StaticResource BoolToFullHalfOpacityConverter}}">
            <ui:CardControl x:Name="TaskbarVisualizerEnabledTitleCard" Tag="Indexable" Icon="{ui:SymbolIcon SoundWaveCircle24}" Margin="0,0,0,24">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{DynamicResource TaskbarVisualizerEnabledTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                            <fluentflyoutcontrols:AccentBadge Margin="6,0,0,0" />
                        </StackPanel>
                        <TextBlock Text="{DynamicResource TaskbarVisualizerEnabledDescription}" FontSize="12" Opacity="0.5" />
                    </StackPanel>
                </ui:CardControl.Header>
                <ui:ToggleSwitch IsChecked="{Binding TaskbarVisualizerEnabled, Mode=TwoWay}" IsEnabled="{Binding IsPremiumUnlocked}" />
            </ui:CardControl>

            <ui:CardControl x:Name="TaskbarVisualizerPositionTitleCard" Tag="Indexable" Icon="{ui:SymbolIcon ArrowMove24}" Margin="0,0,0,3">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="{DynamicResource TaskbarVisualizerPositionTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                    </StackPanel>
                </ui:CardControl.Header>
                <ComboBox SelectedIndex="{Binding TaskbarVisualizerPosition}" IsEnabled="{Binding IsPremiumUnlocked}" Style="{StaticResource DefaultComboBoxStyle}">
                    <ComboBoxItem Content="{DynamicResource PositionBottomLeft}" />
                    <ComboBoxItem Content="{DynamicResource PositionBottomRight}" />
                </ComboBox>
            </ui:CardControl>

            <ui:CardControl x:Name="TaskbarVisualizerBarCountTitleCard" Tag="Indexable" Icon="{ui:SymbolIcon DataBarVertical24, Filled=True}" Margin="0,0,0,3">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{DynamicResource TaskbarVisualizerBarCountTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                        </StackPanel>
                        <TextBlock Text="{DynamicResource TaskbarVisualizerBarCountDescription}" FontSize="12" Opacity="0.5" />
                    </StackPanel>
                </ui:CardControl.Header>
                <StackPanel Orientation="Horizontal" Opacity="{Binding IsPremiumUnlocked, Converter={StaticResource BoolToFullHalfOpacityConverter}}" >
                    <Slider Minimum="1" Maximum="20" Value="{Binding TaskbarVisualizerBarCount, Mode=TwoWay}" IsEnabled="{Binding IsPremiumUnlocked}" TickFrequency="1" IsSnapToTickEnabled="True" Width="150"
                            IsMoveToPointEnabled="True" />
                    <ui:TextBlock Text="{Binding TaskbarVisualizerBarCount}" Width="24" TextAlignment="Center" Margin="12,0,0,0" />
                </StackPanel>
            </ui:CardControl>

            <ui:CardControl x:Name="TaskbarVisualizerWidthTitleCard" Tag="Indexable" Icon="{ui:SymbolIcon AutoFitWidth24}" Margin="0,0,0,3">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="{DynamicResource TaskbarVisualizerWidthTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                        <TextBlock Text="{DynamicResource TaskbarVisualizerWidthDescription}" FontSize="12" Opacity="0.5" />
                    </StackPanel>
                </ui:CardControl.Header>
                <StackPanel Orientation="Horizontal" Opacity="{Binding IsPremiumUnlocked, Converter={StaticResource BoolToFullHalfOpacityConverter}}" >
                    <Slider Minimum="40" Maximum="140" Value="{Binding TaskbarVisualizerWidth, Mode=TwoWay}" IsEnabled="{Binding IsPremiumUnlocked}" TickFrequency="4" IsSnapToTickEnabled="True" Width="150"
                            IsMoveToPointEnabled="True" />
                    <ui:TextBlock Text="{Binding TaskbarVisualizerWidth}" Width="24" TextAlignment="Center" Margin="12,0,0,0" />
                </StackPanel>
            </ui:CardControl>

            <ui:CardControl x:Name="TaskbarVisualizerStyleTitleCard" Tag="Indexable" Icon="{ui:SymbolIcon PaintBrush24}" Margin="0,0,0,3">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="{DynamicResource TaskbarVisualizerStyleTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                        <TextBlock Text="{DynamicResource TaskbarVisualizerStyleDescription}" FontSize="12" Opacity="0.5" />
                    </StackPanel>
                </ui:CardControl.Header>
                <ComboBox SelectedIndex="{Binding TaskbarVisualizerStyle}" IsEnabled="{Binding IsPremiumUnlocked}" Style="{StaticResource DefaultComboBoxStyle}">
                    <ComboBoxItem Content="{DynamicResource TaskbarVisualizerStyleRounded}" />
                    <ComboBoxItem Content="{DynamicResource TaskbarVisualizerStyleSquare}" />
                    <ComboBoxItem Content="{DynamicResource TaskbarVisualizerStyleThin}" />
                    <ComboBoxItem Content="{DynamicResource TaskbarVisualizerStyleGradient}" />
                    <ComboBoxItem Content="{DynamicResource TaskbarVisualizerStyleSegmented}" />
                    <ComboBoxItem Content="{DynamicResource TaskbarVisualizerStyleWaveform}" />
                </ComboBox>
            </ui:CardControl>

            <ui:CardControl x:Name="TaskbarVisualizerColorModeTitleCard" Tag="Indexable" Icon="{ui:SymbolIcon ColorFill24}" Margin="0,0,0,3">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="{DynamicResource TaskbarVisualizerColorModeTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                        <TextBlock Text="{DynamicResource TaskbarVisualizerColorModeDescription}" FontSize="12" Opacity="0.5" />
                    </StackPanel>
                </ui:CardControl.Header>
                <ComboBox SelectedIndex="{Binding TaskbarVisualizerColorMode}" IsEnabled="{Binding IsPremiumUnlocked}" Style="{StaticResource DefaultComboBoxStyle}">
                    <ComboBoxItem Content="{DynamicResource TaskbarVisualizerColorSingle}" />
                    <ComboBoxItem Content="{DynamicResource TaskbarVisualizerColorAlbumArt}" />
                    <ComboBoxItem Content="{DynamicResource TaskbarVisualizerColorRainbow}" />
                </ComboBox>
            </ui:CardControl>

            <ui:CardControl x:Name="TaskbarVisualizerCenteredBarsTitleCard" Tag="Indexable" Icon="{ui:SymbolIcon AlignCenterHorizontal24, Filled=True}" Margin="0,0,0,3">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="{DynamicResource TaskbarVisualizerCenteredBarsTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                        <TextBlock Text="{DynamicResource TaskbarVisualizerCenteredBarsDescription}" FontSize="12" Opacity="0.5" />
                    </StackPanel>
                </ui:CardControl.Header>
                <ui:ToggleSwitch IsChecked="{Binding TaskbarVisualizerCenteredBars, Mode=TwoWay}" IsEnabled="{Binding IsPremiumUnlocked}" />
            </ui:CardControl>

            <ui:CardExpander x:Name="TaskbarVisualizerBaselineTitleCard" Tag="Indexable" Icon="{ui:SymbolIcon LineHorizontal120}" ContentPadding="0" Margin="0,0,0,12">
                <ui:CardExpander.Header>
                    <Grid ColumnDefinitions="*, Auto">
                        <StackPanel Orientation="Vertical" Grid.Column="0">
                            <TextBlock Text="{DynamicResource TaskbarVisualizerBaselineTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                            <TextBlock Text="{DynamicResource TaskbarVisualizerBaselineDescription}" FontSize="12" Opacity="0.5" />
                        </StackPanel>
                        <ui:ToggleSwitch Grid.Column="1" IsChecked="{Binding TaskbarVisualizerBaseline, Mode=TwoWay}" IsEnabled="{Binding IsPremiumUnlocked}" Margin="0,0,16,0" />
                    </Grid>
                </ui:CardExpander.Header>
                <StackPanel Opacity="{Binding TaskbarVisualizerBaseline, Converter={StaticResource BoolToFullHalfOpacityConverter}}">
                    <ui:CardControl Background="Transparent" BorderBrush="{ui:ThemeResource CardStrokeColorDefaultSolidBrush}" BorderThickness="0" CornerRadius="0" Padding="52,12,16,12">
                        <ui:CardControl.Header>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{DynamicResource TaskbarWidgetAutoHideTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                                <TextBlock Text="{DynamicResource TaskbarVisualizerBaselineAutoHideDescription}" FontSize="12" Opacity="0.5" />
                            </StackPanel>
                        </ui:CardControl.Header>
                        <ui:ToggleSwitch IsChecked="{Binding TaskbarVisualizerBaselineAutoHide, Mode=TwoWay}" IsEnabled="{Binding TaskbarVisualizerBaseline}" />
                    </ui:CardControl>
                </StackPanel>
            </ui:CardExpander>

            <ui:CardControl x:Name="TaskbarVisualizerAudioSensitivityTitleCard" Tag="Indexable" Icon="{ui:SymbolIcon BezierCurveSquare20, Filled=False}" Margin="0,0,0,3">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="{DynamicResource TaskbarVisualizerAudioSensitivityTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                        <TextBlock Text="{DynamicResource TaskbarVisualizerAudioSensitivityDescription}" FontSize="12" Opacity="0.5" />
                    </StackPanel>
                </ui:CardControl.Header>
                <StackPanel Orientation="Horizontal" Opacity="{Binding IsPremiumUnlocked, Converter={StaticResource BoolToFullHalfOpacityConverter}}" >
                    <ui:SymbolIcon Symbol="Subtract24" Width="24" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    <Slider Minimum="1" Maximum="3" Value="{Binding TaskbarVisualizerAudioSensitivity, Mode=TwoWay}" IsEnabled="{Binding IsPremiumUnlocked}" TickFrequency="1" TickPlacement="Both" IsSnapToTickEnabled="True" Width="150" Margin="12,0,12,0"
                            IsMoveToPointEnabled="True" />
                    <ui:SymbolIcon Symbol="Add24" Width="24" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </StackPanel>
            </ui:CardControl>

            <ui:CardControl x:Name="TaskbarVisualizerAudioPeakLevelTitleCard" Tag="Indexable" Icon="{ui:SymbolIcon ArrowAutofitUp24, Filled=False}" Margin="0,0,0,3">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{DynamicResource TaskbarVisualizerAudioPeakLevelTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                        </StackPanel>
                        <TextBlock Text="{DynamicResource TaskbarVisualizerAudioPeakLevelDescription}" FontSize="12" Opacity="0.5" />
                    </StackPanel>
                </ui:CardControl.Header>
                <StackPanel Orientation="Horizontal" Opacity="{Binding IsPremiumUnlocked, Converter={StaticResource BoolToFullHalfOpacityConverter}}" >
                    <ui:SymbolIcon Symbol="Subtract24" Width="24" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    <Slider Minimum="1" Maximum="3" Value="{Binding TaskbarVisualizerAudioPeakLevel, Mode=TwoWay}" IsEnabled="{Binding IsPremiumUnlocked}" TickFrequency="1" TickPlacement="Both" IsSnapToTickEnabled="True" Width="150" Margin="12,0,12,0"
                            IsMoveToPointEnabled="True" />
                    <ui:SymbolIcon Symbol="Add24" Width="24" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </StackPanel>
            </ui:CardControl>

            <ui:CardControl x:Name="TaskbarVisualizerOutputDeviceCard" Tag="Indexable" Icon="{ui:SymbolIcon Speaker224}" Margin="0,0,0,12">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <TextBlock FontSize="14" FontWeight="Regular" VerticalAlignment="Center">
                            <Hyperlink x:Name="StartupHyperlink" Tag="Indexable" NavigateUri="ms-settings:apps-volume" BaselineAlignment="Center"
                                   RequestNavigate="StartupHyperlink_RequestNavigate" TextDecorations="None" Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" >
                                <Run Text="{DynamicResource TaskbarVisualizerOutputDeviceTitle}" />
                                <ui:SymbolIcon VerticalAlignment="Center" FontSize="14" Symbol="ArrowUpRight24" />
                            </Hyperlink>
                        </TextBlock>
                        <TextBlock Text="{DynamicResource TaskbarVisualizerOutputDeviceDescription}" FontSize="12" Opacity="0.5" />
                    </StackPanel>
                </ui:CardControl.Header>
            </ui:CardControl>

            <ui:CardControl x:Name="TaskbarVisualizerClickableTitleCard" Tag="Indexable" Icon="{ui:SymbolIcon ExpandUpRight24}" Margin="0,0,0,76">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{DynamicResource TaskbarVisualizerClickableTitle}" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                        </StackPanel>
                        <TextBlock Text="{DynamicResource TaskbarVisualizerClickableDescription}" FontSize="12" Opacity="0.5" />
                    </StackPanel>
                </ui:CardControl.Header>
                <ui:ToggleSwitch IsChecked="{Binding TaskbarVisualizerClickable, Mode=TwoWay}" IsEnabled="{Binding IsPremiumUnlocked}" />
            </ui:CardControl>
        </StackPanel>
    </StackPanel>
</Page>
